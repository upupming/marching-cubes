#include "marching_cubes.h"

MarchingCubes::MarchingCubes(const unsigned short* data, double x_spacing, double y_spacing, double z_spacing) : data(data), x_spacing(x_spacing), y_spacing(y_spacing), z_spacing(z_spacing) {
}
